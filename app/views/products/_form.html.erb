<%= simple_form_for @product , html: { multipart: true } do |f| %>
  <% if @product.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@product.errors.count, "error") %> prohibited this product from being saved:</h2>

      <ul>
      <% @product.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="col-sm-6">
    <div class="form-group">
      <%= f.label :name %>
      <%= f.text_field :name, class: "form-control input-lg" %>
      <small>Add a name to your item. eg. iPhone 7 32GB</small>
    </div>

    <div class="form-group">
      <%= f.label :category_id %>
      <%= f.collection_select :category_id, Category.order(:name), :id, :name, {include_blank: true}, {class: "form-control input-lg"} %>
      <small>Select a Category</small>
    </div>

    <div class="form-group">
      <%= f.label :subcategory_id %>
      <%= f.grouped_collection_select :subcategory_id, Category.order(:name), :subcategories, :name, :id, :name, {include_blank: true}, {class: "form-control input-lg"} %>
      <small>Select a Subcategory</small>
    </div>

    <div class="form-group">
      <%= f.label :description %>
      <%= f.text_area :description, class: "form-control input-lg" %>
      <small>Provide a very good description for your item. Remember good description attracts!</small>
    </div>

    <div class="form-group">
      <%= f.label :price %>
      <%= f.text_field :price, class: "form-control input-lg" %>
      <small>Add your item price per unit.</small>
    </div>
  </div>

  <div class="col-sm-6">
    <div class="form-group">
      <%= f.label :stock %>
      <%= f.text_field :stock, class: "form-control input-lg" %>
      <small>How many of this product do you have to offer?</small>
    </div>

    <div class="pdt-assets">
        <%= f.label :images %>
        <%= f.file_field :images, multiple: true %>
        <small>You can select up to 5 images, the first will be used as the feature image.</small>
    </div>
  </div>

  <div class="actions">
    <%= f.submit class: "btn btn-danger btn-block btn-lg" %>
  </div>
  <hr>
<% end %>
