
<div class="container">
    <div class="row">
        <div class="col-sm-8 col-sm-offset-2">
            <div class="text-center">
                <p class="lead">Welcome <%= @email %>!</p>

                <p>Welcome to new shopping experience:</p>

                <p><%= link_to 'Confirm my account', confirmation_url(@resource, confirmation_token: @token), class: "btn btn-danger btn-block btn-lg" %></p>
            </div>
        </div>
    </div>
</div>
