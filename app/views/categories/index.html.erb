

<section class="sect mt10">
    <div class="container">
        <div class="panel panel-default">
            <div class="panel-body">
                <% @categories.each do |c| %>

                    <h3><%= link_to c.name, category_path(c) %></h3>

                <% end %>
            </div>
        </div>
    </div>
</section>


